class PlayerChoice{
    static playerChoice(choice){
        if (choice !== "" && choice !== undefined){  
        this.choice = choice;
        // console.log(this.choice);
        return this.choice;
    }
    }
}
